---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: backend-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`backend.rajubk.com`)
      kind: Rule
      services:
        - name: backend-service
          port: 80
      middlewares:
        - name: authentik
  tls:
    secretName: test-rajubk-com-tls
